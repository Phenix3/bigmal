app_backend_product_category:
    resource: |
        alias: app.product.category
        section: backend
        except: ['show']
        redirect: update
        grid: app_backend_product_category
        vars:
            all:
                subheader: app.ui.manage_articles
            index:
                icon: newspaper
        templates: backend/crud
    type: sylius.resource