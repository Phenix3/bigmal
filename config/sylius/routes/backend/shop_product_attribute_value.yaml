app_backend_product_attribute_value:
    resource: |
        alias: app.product.attribute_value
        section: backend
        except: ['show']
        redirect: update
        grid: app_backend_product_attribute_value
        vars:
            all:
                subheader: app.ui.manage_articles
            index:
                icon: newspaper
        templates: backend/crud
    type: sylius.resource