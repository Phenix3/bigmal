app_backend_product_product:
    resource: |
        alias: app.product.product
        section: backend
        except: ['show']
        redirect: update
        grid: app_backend_product_product
        vars:
            all:
                subheader: app.ui.manage_articles
            index:
                icon: newspaper
        templates: backend/crud
    type: sylius.resource